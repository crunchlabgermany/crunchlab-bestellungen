import { authenticate } from "../shopify.server";import db from "../db.server";export const loader=async({request,params})=>{const{session}=await authenticate.admin(request);const asset=await db.socialMediaAsset.findFirst({where:{id:Number(params.id),shop:session.shop,status:{not:"DELETED"}},include:{blob:true}});if(!asset?.blob)throw new Response("Nicht gefunden",{status:404});return new Response(asset.blob.data,{headers:{"Content-Type":asset.mimeType,"Content-Length":String(asset.fileSize),"Cache-Control":"private, max-age=3600","X-Content-Type-Options":"nosniff"}})};
