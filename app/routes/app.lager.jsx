import { useLoaderData } from "react-router"; import { authenticate } from "../shopify.server"; import db from "../db.server";
export const loader=async({request})=>{const{session}=await authenticate.admin(request);return{ingredients:await db.ingredient.findMany({where:{shop:session.shop},orderBy:[{category:"asc"},{name:"asc"}]})}};
export default function Lager(){const{ingredients}=useLoaderData();return <s-page heading="Lager"><s-section><s-banner tone="info">Bestandskorrekturen erfolgen weiterhin sicher in der Zutatenverwaltung. Reservierungen und Verbrauch werden hier transparent ausgewiesen.</s-banner>{ingredients.map(i=><p key={i.id}><strong>{i.name}</strong> · {i.stockWeight.toLocaleString("de-DE")} g {i.stockWeight<=i.minimumStock&&"⚠ Mindestbestand erreicht"}</p>)}</s-section></s-page>}
